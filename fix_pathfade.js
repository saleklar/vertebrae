const fs = require("fs"); let code = fs.readFileSync("src/Scene3D.tsx", "utf8"); code = code.replace(/let pathFade = 1.0;\\n/g, ""); code = code.replace(/\\s*pathFade = Math\\.min\\(pathT \\* 20\\.0, \\(1\\.0 \\- pathT\\) \\* 20\\.0, 1\\.0\\); \\/\\/ fade in\\/out\\n/g, ""); const search = "const tendrilSeed  = slotSeed + Math.floor((fAnimT + birthOffset) / lifespan) * 1.618;"; const rep = "let pathFade = 1.0;\\n            if (pathCurveF) {\\n              const pathT = ( (numTendrils > 1 ? ti / (numTendrils - 1) : 0) + (fAnimT * Math.max(0.1, speed) * (fp.pathSpeed ?? 0.05)) ) % 1.0;\\n              pathFade = Math.min(pathT * 20.0, (1.0 - pathT) * 20.0, 1.0);\\n            }\\n            const tendrilSeed  = slotSeed + Math.floor((fAnimT + birthOffset) / lifespan) * 1.618;"; code = code.replace(search, rep); fs.writeFileSync("src/Scene3D.tsx", code, "utf8"); console.log("Fixed scopes");
