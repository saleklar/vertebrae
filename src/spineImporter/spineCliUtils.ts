// Utility for running Spine CLI and extracting JSON
export async function extractSpineJson(spineFilePath: string, outputJsonPath: string): Promise<void> {
  // TODO: Implement CLI call
  // Example: spine --export --output outputJsonPath spineFilePath
}
